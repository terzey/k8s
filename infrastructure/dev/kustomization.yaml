apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../base
  - prometheus/alertmanager-config.yaml

patchesStrategicMerge:
  - prometheus/alertmanager.yaml
  - prometheus/prometheus.yaml
  - prometheus/prometheus-adapter.yaml